export async function generateInvoicePDF(_order: any): Promise<string> {
  // ─── PDF generation removed ────────────────────────────────────
  // الـ invoice details بتتبعت في الـ email مباشرة
  // الـ function اتحتفظ بيها عشان مش نكسر أي imports قديمة
  return '';
}